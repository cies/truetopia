- throw_content(:title, 'projecten, Overzicht')

.subcolumns
  .c25l
    .subcl
      %h4
        Acties
      .column_menu
        = link_to "&nbsp;&nbsp;Maak een nieuw project&nbsp;", url(:new_project)
        %br/
        = link_to "&nbsp;&nbsp;Zoek een project&nbsp;", url(:search)
        %br/
        = link_to "&nbsp;&nbsp;Recente wijzigingen&nbsp;", '/recent'
        %br/
      %br/
      %h4
        Projecten
      %p
        Lorem ipsum dolor sit amet, consectetuer adipiscing elit. Nullam malesuada   suscipit pede. Nullam ipsum lacus, varius vel, nonummy in, consequat ut, neque.   Vivamus viverra. Duis dolor arcu, lacinia sit amet, sollicitudin sed.
  .c75r
    .subcr
      %h1
        Overzicht van projecten
      - if @projects.any?
        %h3
          Recent gewijzigde projecten
        - @projects.each do |project|
          .subpage
            .subpage_header
              %span.title
                - title_html= capture_haml do
                  %span.number= '#' + project.id.to_s
                  = project.project_document.current_version.title
                = link_to title_html, url(:project, :project_id => project.id)
              .meta
                = render_relative_date project.created_at
                gestart door
                = link_to project.user.login, url(:user, :login => project.user.login)
                %br/
                huidige versie
                = project.project_document.version_count
                verscheen
                = render_relative_date project.project_document.current_version.created_at
                door
                = link_to project.project_document.current_version.user.login, url(:user, :login => project.project_document.current_version.user.login)
            .subpage_content
              = (html_escape project.project_document.current_version.content).gsub("\n", '<br/>')[1..200].strip + '...'
            .subpage_footer
              %strong
                = link_to 'lees verder...', url(:project, :project_id => project.id)


      - else
        Eerst ff
        = link_to 'eentje',  url(:new_project)
        maken goofie..
