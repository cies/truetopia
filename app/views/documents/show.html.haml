- throw_content :title, "document ##{@document.id} #{@document.current_version.title}"
= partial 'documents/document_header', :document => @document, :current => :show
.subcolumns.right_bg
  .c75r
    .subcr
      .content
        = @document.current_version.content_html
      .footer
  .c25l
    .subcl.sidebar
      = partial 'document_sidebar', :document => @document
